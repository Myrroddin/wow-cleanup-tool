# WoW Cleanup Tool - External Dependencies
# These packages are automatically installed by the application if missing,
# but can also be manually installed with: pip install -r requirements.txt

# Hardware detection (CPU, GPU, RAM) for Game Optimizer
psutil>=5.0.0

# Safe file deletion to Recycle Bin/Trash
send2trash>=1.5.0

# Image processing for UI elements (checkboxes, icons)
Pillow>=8.0.0
