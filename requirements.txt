# WoW Cleanup Tool - Required Dependencies
# These packages are automatically installed by the application on first run.
# You can also manually install them with: pip install -r requirements.txt

# Hardware detection (CPU, GPU, RAM) for Game Optimizer
psutil>=5.0.0

# Safe file deletion to Recycle Bin/Trash
send2trash>=1.5.0

# Image processing for UI elements (checkboxes, icons, screenshot preview)
Pillow>=8.0.0

# Optional: For building standalone executables (not needed to run the app)
# Uncomment the line below if you want to build executables locally
# pyinstaller>=6.0.0
